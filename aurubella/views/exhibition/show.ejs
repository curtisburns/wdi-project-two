<h1><%= image.userUpload %></h1>
<img src="/images/<%= image.imageURL %>" alt="<%= image.caption %>">

<h2><%= image.dateOrTimeUploaded %></h1>
<h2><%= image.caption %></h2>
<% image.tags.forEach(tag => { %>
  <a href='#'> <%= tag %> </a>
<% }) %>

<h2><%= image.likes %></h2>

<a class="button" href="/exhibition/<%= image.id %>/edit">Edit</a>

<%#need to make it so that you can only edit and delete your own photos %>
<form class="field" action="/exhibition/<%= image.id %>" method="POST">
  <button>Delete</button>
  <input type="hidden" name="_method" value="DELETE">
</form>

<%- include('../comments/index', { image }) %>
<%- include('../comments/new', { image }) %>
